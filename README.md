# Sessions

Install dependencies

```
bin/bundle
```

Create database

```
bin/rake db:create
```

Run database migrations

```
bin/rake db:migrate
```

Run server on port 3000

```
bin/rails s -p 3000
```
